# Base URL of the backend API. Required for production builds (see vite.config.ts) —
# `npm run build` fails fast if it is unset instead of silently shipping a build
# that points at http://localhost:5000/api.
VITE_API_BASE_URL=http://localhost:5000/api
